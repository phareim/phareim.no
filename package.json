{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --host",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/node": "^18",
    "nuxt": "^3.13.1"
  },
  "dependencies": {
    "@nuxtjs/tailwindcss": "^6.12.1",
    "firebase": "^11.2.0",
    "openai": "^4.80.0"
  },
  "overrides": {
    "tr46": "4.0.0"
  }
}
