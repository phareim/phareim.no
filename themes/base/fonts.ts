/**
 * The machine face as a canvas-ready family list. Keep in sync with
 * --font-machine in ./fonts.css (canvas cannot read CSS variables).
 */
export const MACHINE_FONT = '"Space Mono", ui-monospace, "Courier New", monospace'
